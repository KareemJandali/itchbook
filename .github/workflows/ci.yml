name: ci

# Every branch, not just main. Restricting this to main meant four phases of
# work were pushed to a feature branch without CI ever running, and the first
# run after merging failed on a warning clang catches and gcc does not.
on:
  push:
  pull_request:

jobs:
  # The README says which toolchain it was verified on, so CI has to be the
  # thing that verifies it. This job exists only to keep that sentence true:
  # gcc catches warnings clang does not and vice versa, and a second compiler
  # is the cheapest evidence that the code is portable rather than tuned to
  # one frontend. It builds and runs the unit tests; the long soaks and the
  # analysis stay in the clang job rather than being paid for twice.
  gcc-build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install deps
        run: sudo apt-get update && sudo apt-get install -y cmake ninja-build zlib1g-dev g++

      - name: Configure (Debug + sanitizers)
        env:
          CC: gcc
          CXX: g++
        run: cmake -S . -B build-gcc -G Ninja -DCMAKE_BUILD_TYPE=Debug

      - name: Build
        run: cmake --build build-gcc

      - name: Unit tests (C++)
        run: ctest --test-dir build-gcc --output-on-failure

  # A fourth job, because two compilers on one platform is not two platforms.
  #
  # This exists because of a specific failure, and the failure is instructive
  # about what the gcc job above can and cannot prove. On Linux x86-64 `size_t`
  # and `uint64_t` are BOTH `unsigned long` -- the same type -- so
  #
  #     std::printf("%" PRIu64, some.size());
  #
  # is not merely accepted by gcc and clang, it is CORRECT there, and -Wformat
  # has nothing to say. On macOS `uint64_t` is `unsigned long long` while
  # `size_t` stays `unsigned long`; the same line is a -Werror=format build
  # failure. tools/wire_to_book.cpp carried exactly that line from phase 10.6
  # until it was found by running the local gate on a Mac -- through every
  # green CI run in between, because no amount of Linux compilers can see it.
  #
  # Two compilers catch warnings that differ by front end. Two PLATFORMS catch
  # assumptions that differ by data model, and a repository whose README says
  # "C++20" rather than "C++20 on Linux" needs the second kind too.
  #
  # Build and unit tests only. The soaks, the analysis and the UDP work stay on
  # Linux rather than being paid for twice; what is bought here is the front
  # end over a different data model.
  macos-build-and-test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install deps
        run: brew install ninja || true

      - name: Configure (Debug + sanitizers)
        run: cmake -S . -B build-macos -G Ninja -DCMAKE_BUILD_TYPE=Debug

      - name: Build
        run: cmake --build build-macos

      - name: Unit tests (C++)
        run: ctest --test-dir build-macos --output-on-failure

      # The regression this job was added for, pinned so it cannot come back
      # quietly. A size_t formatted with PRIu64 compiles clean on Linux and
      # fails here, so the grep is a belt to the -Werror braces: it names the
      # mistake rather than leaving the next person to decode a format warning.
      - name: No size_t formatted as PRIu64
        run: |
          if grep -rn 'PRIu64[^;]*\.\(size\|books\|capacity\|length\)()' \
               tools/ include/ tests/ 2>/dev/null | grep -v static_cast; then
            echo "FAIL: a size_t is being formatted with PRIu64. Use %zu."
            exit 1
          fi
          echo "no size_t/PRIu64 pairs"

  # A third job, because ThreadSanitizer and AddressSanitizer cannot share a
  # binary. Phase 10 is the first code here with two threads in it, and a
  # lock-free structure that has never been under a race detector is an
  # assertion rather than a result: the ring's whole safety argument is one
  # release/acquire pair, and x86-64's strong memory model would hide a missing
  # release behind correct-looking output for as long as anyone cared to run it.
  tsan-spsc-ring:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install deps
        run: sudo apt-get update && sudo apt-get install -y cmake ninja-build zlib1g-dev g++

      # Before trusting a green TSan job, prove TSan is armed. A runner without
      # the runtime, or a flag that silently did not apply, produces a job that
      # passes because it detected nothing -- which looks identical to a job
      # that passes because there was nothing to detect. This program has a
      # deliberate race and the step FAILS if the sanitizer does not catch it.
      - name: Prove the race detector detects races
        run: |
          cat > /tmp/race.cpp <<'CPP'
          #include <thread>
          int shared = 0;
          int main() {
            std::thread t([]{ for (int i = 0; i < 100000; ++i) shared++; });
            for (int i = 0; i < 100000; ++i) shared++;
            t.join();
          }
          CPP
          g++ -std=c++20 -fsanitize=thread -fno-omit-frame-pointer -g -O1 -o /tmp/race /tmp/race.cpp
          if /tmp/race 2>/tmp/race.err; then
            echo "TSan did not flag a deliberate data race -- the detector is not armed."
            cat /tmp/race.err
            exit 1
          fi
          grep -q "WARNING: ThreadSanitizer: data race" /tmp/race.err
          echo "TSan armed: a deliberate race is caught and the process exits non-zero."

      - name: Configure (ThreadSanitizer)
        env:
          CC: gcc
          CXX: g++
        run: cmake -S . -B build-tsan -G Ninja -DCMAKE_BUILD_TYPE=RelWithDebInfo -DITCHBOOK_TSAN=ON

      - name: Build
        run: |
          cmake --build build-tsan --target test_spsc_ring test_reader_thread spsc_stress
          cmake --build build-tsan --target wire_to_book mold_replay_udp mold_wrap

      - name: Ring unit tests, race-detected
        run: ./build-tsan/test_spsc_ring

      # The second consumer of the ring, and the first producer that is not a
      # socket. Its chunk-boundary cases drive the ring through carry-and-
      # publish patterns the socket path never produces.
      - name: Reader thread, race-detected
        env:
          TSAN_OPTIONS: halt_on_error=1
        run: ./build-tsan/test_reader_thread

      # halt_on_error stops at the first race rather than reporting thousands of
      # instances of one bug. The stress run randomises batch sizes and stalls
      # both threads at random points, including between writing a slot and
      # publishing it -- the exact window the release store closes.
      #
      # It also fails if the ring never reached full AND never reached empty: a
      # run that stayed comfortably in the middle exercised no boundary and
      # proves nothing, however many operations it did.
      - name: Ring stress, race-detected
        env:
          TSAN_OPTIONS: halt_on_error=1
        run: ./build-tsan/spsc_stress --ops 20000000 --seed 3

      # The stress test drives the ring with two threads that do nothing else.
      # This drives it the way the system actually does: a producer whose pace
      # is set by a socket, a consumer that parses and mutates books between
      # pops, and the publish/consume batching that only the real receiver
      # performs. A missing release store would be invisible in both on x86 and
      # is exactly what this job exists to catch, so the real consumer belongs
      # under the detector and not only the synthetic one.
      #
      # The rate is low and the ring large on purpose: under TSan every memory
      # access is instrumented, and the interesting property here is the
      # handoff, not the throughput.
      - name: The real pipeline under the race detector
        env:
          TSAN_OPTIONS: halt_on_error=1
        run: |
          python3 scripts/make-synthetic-feed.py /tmp/tsan-feed.gz --messages 100000
          ./build-tsan/mold_wrap /tmp/tsan-feed.gz /tmp/tsan-feed.pkt.gz >/dev/null
          ./build-tsan/wire_to_book --port 26701 --ring-log2 16 --timeout-ms 8000 \
            --rcvbuf-mb 16 --expect-messages 200000 --json /tmp/tsan-wire.json \
            > /tmp/tsan-wire.txt 2>&1 &
          pid=$!
          for _ in $(seq 1 300); do
            if grep -q '^READY' /tmp/tsan-wire.txt; then break; fi
            sleep 0.1
          done
          ./build-tsan/mold_replay_udp /tmp/tsan-feed.pkt.gz --host 127.0.0.1 \
            --port 26701 --rate 20000 --quiet
          rc=0
          wait $pid || rc=$?
          cat /tmp/tsan-wire.txt
          if grep -q "WARNING: ThreadSanitizer" /tmp/tsan-wire.txt; then
            echo "FAIL: ThreadSanitizer flagged the pipeline"
            exit 1
          fi
          # 0 or 3. This step is about RACES, and exit 3 is a capacity verdict:
          # the pipeline worked exactly as designed and lost datagrams under an
          # offered load it could not absorb. Under ThreadSanitizer -- which
          # instruments every memory access -- that is a live possibility at any
          # rate, and it became more live once the kernel-drop half of the LOSSY
          # gate started working: before, wire_to_book read /proc/net/udp after
          # close(fd), found no row, and reported a constant 0. A race-detector
          # step must not go red because the sanitised build was slow.
          if [[ "$rc" != 0 && "$rc" != 3 ]]; then
            echo "FAIL: wire_to_book exited $rc under TSan"
            exit 1
          fi
          echo "pipeline race-clean end to end (exit $rc)"

  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install deps
        run: sudo apt-get update && sudo apt-get install -y cmake ninja-build zlib1g-dev clang

      - name: Configure (Debug + sanitizers)
        env:
          CC: clang
          CXX: clang++
        run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug

      - name: Build
        run: cmake --build build

      - name: Unit tests (C++)
        run: ctest --test-dir build --output-on-failure

      - name: Unit tests (Python oracle)
        run: python3 -m unittest discover -s tests -p 'test_*.py' -v

      - name: Smoke test (generate sample, run tools)
        run: |
          python3 python/make_sample.py data/raw/sample.gz
          ./build/itch_census data/raw/sample.gz
          ./build/itch_dump   data/raw/sample.gz 5
          ./build/itch_slice  data/raw/sample.gz TEST data/sliced/TEST.gz

      # The census counts live orders with a structure that shares no code with
      # the book: no levels, no pool, twelve bytes per order instead of forty.
      # So the two arrive at "how many orders are resting" by different routes,
      # and phase 9 will lean on the census number to size a ref map for 8,700
      # symbols at once. Two independent counts of the same feed must agree, or
      # neither of them gets to be evidence.
      - name: Census and book agree on how many orders are live
        run: |
          python3 python/make_queue_feed.py data/raw/live.gz --messages 200000 \
            --gap-ns 20000 --seed 3
          ./build/itch_census  data/raw/live.gz --peak-orders | tee /tmp/census.txt
          ./build/book_replay  data/raw/live.gz --symbol TEST --json /tmp/book.json
          python3 - <<'PY'
          import json, re, sys
          census = open('/tmp/census.txt').read()
          live = int(re.search(r'live at end of file\s+(\d+)', census).group(1))
          unknown = int(re.search(r'references naming no live order\s+(\d+)', census).group(1))
          assert re.search(r'inserted == live \+ removed \+ emptied\s+yes', census), census
          book = json.load(open('/tmp/book.json'))
          print(f"census live={live} unknown={unknown} | "
                f"book resting={book['resting_orders']} unknown={book['unknown_refs']}")
          if live != book['resting_orders'] or unknown != book['unknown_refs']:
              sys.exit("census and book disagree about the same feed")
          PY

      # Phase 9 routes a whole feed into one BookSet. Nothing about that is
      # supposed to change what an individual book does -- the mutations are
      # the same code, reached through one more switch -- so the way to find
      # out whether routing broke a symbol is to reconstruct it both ways and
      # diff. A shared pool handing out the wrong node, a message routed to a
      # neighbour, a counter that became the market's instead of the symbol's:
      # none of those are visible to a run that only ever builds one book.
      #
      # Then one of those symbols is sliced out and graded against the Python
      # oracle, so the chain closes: all-symbols == single-symbol == oracle.
      # Phase 9 rewrote the inside of the book: the storage moved out from
      # under it, every order gained a field, and messages now arrive through a
      # router. None of that is supposed to change what a reconstruction
      # produces. This records a known feed's output and compares it byte for
      # byte -- snapshots, summary, and the all-symbols rows -- and it checks
      # the FEED's hash first, because a generator whose output moved and a
      # book whose output moved fail in the same place and mean opposite
      # things.
      # Standing rule 7: no number reaches a document by being retyped. The
      # census block in validation/README.md is generated from the committed
      # census JSON, and this fails if the prose has drifted from the data.
      - name: The census block matches the census
        run: python3 scripts/census-report.py --check

      # The phase 9 write-up is generated from the run's own artifacts. Its
      # numbers cannot drift from the run that produced them, because CI fails
      # if the committed prose disagrees with the committed data.
      - name: The phase 9 results match their artifacts
        run: python3 scripts/phase9-report.py --check

      # Phase 10 subtracts a timestamp taken on one core from one taken on
      # another, which nothing in this repository had ever done. This keeps the
      # measuring tool alive; it asserts nothing about the runner's clocks,
      # because a shared CI machine cannot support a claim about core
      # synchronisation. The number that matters is measured on the hardware the
      # results come from and quoted in docs/phase10-methodology.md.
      # The paced sender, end to end through a real socket. This asserts the
      # packets arrive and that the tool reports its own lateness; it asserts
      # NOTHING about the timing, because a shared runner cannot hold a
      # schedule and the tool's whole point is to say so rather than pretend.
      - name: Paced UDP sender delivers every packet
        run: |
          python3 python/make_queue_feed.py data/raw/pace.gz --messages 40000 \
            --seed 4 --gap-ns 20000
          ./build/mold_wrap data/raw/pace.gz data/raw/pace.mold.gz --mtu 1400
          python3 - <<'PY' &
          import socket, json
          s = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)
          s.setsockopt(socket.SOL_SOCKET, socket.SO_RCVBUF, 8 << 20)
          s.bind(("127.0.0.1", 26400))
          s.settimeout(10.0)
          n = 0
          try:
              while True:
                  s.recvfrom(65535)
                  n += 1
          except socket.timeout:
              pass
          json.dump({"received": n}, open("/tmp/sink.json", "w"))
          PY
          sleep 1
          ./build/mold_replay_udp data/raw/pace.mold.gz --rate 100000 \
            --json /tmp/send.json
          wait
          python3 -c "
          import json, sys
          sent = json.load(open('/tmp/send.json'))
          got = json.load(open('/tmp/sink.json'))
          print(f\"sent {sent['packets']} packets / {sent['messages']} messages, \"
                f\"sink received {got['received']}\")
          assert sent['send_errors'] == 0, 'sender reported send errors'
          assert sent['packets'] > 0 and sent['messages'] > 0
          if got['received'] != sent['packets']:
              print('note: kernel dropped %d packets on loopback' %
                    (sent['packets'] - got['received']))
          "

      # 10.7's sender tail was a scheduling tail, not a pacing error: p50
      # lateness of 35 ns against a 1.09 ms max is a thread being taken off the
      # CPU, and spinning does not help when you are not running. So the sender
      # can ASK for SCHED_FIFO -- and the only thing asserted here is that
      # asking and getting stay distinguishable. A run that silently failed to
      # get real-time priority and one that never asked produce identical
      # lateness, so a report that conflated them would put a number in a table
      # meaning something it does not.
      #
      # No outcome is asserted: this runner is unprivileged and a self-hosted
      # root one is not, and the point is that either way the artifact says
      # which happened. On macOS the calls do not exist at all and the tool
      # reports that rather than pretending.
      - name: The sender asks for real-time priority, and says what it got
        run: |
          ./build/mold_replay_udp data/raw/pace.mold.gz --rate 100000 \
            --host 127.0.0.1 --port 26411 --rt-priority 50 --json /tmp/rt.json \
            > /tmp/rt.txt 2>&1
          cat /tmp/rt.txt | head -5
          python3 -c "
          import json
          rt = json.load(open('/tmp/rt.json'))['realtime']
          assert rt['requested'] is True, rt
          assert rt['priority'] == 50, rt
          # Granted or refused, but never ambiguous: a refusal must carry the
          # reason it was refused.
          if not rt['scheduler_granted']:
              assert rt['scheduler_error'], 'refused without saying why'
              print('SCHED_FIFO refused here, and said so:', rt['scheduler_error'])
          else:
              print('SCHED_FIFO granted at priority', rt['priority'])
          "

      - name: Cross-core clock offset tool runs
        run: |
          ./build/tsc_offset --samples 20000 --cpu-a 0 --cpu-b 1 --json /tmp/tsc.json
          python3 -c "
          import json
          d = json.load(open('/tmp/tsc.json'))
          for k in ('timestamp_source','pinned','resolution_ns','resolvable'):
              assert k in d, k
          print('offset tool reports:', d['timestamp_source'],
                '| resolvable' if d['resolvable'] else '| bounded at %.0f ns' % d['resolution_ns'])
          "

      # Phase 10.4/10.5: the first latency in this repository that crosses a
      # thread boundary, and therefore the first that can lose a message. Two
      # runs. The first must deliver every message with zero drops at three
      # layers -- kernel, ring, and mid-block -- and the second must lose
      # thousands on purpose and account for every one of them.
      #
      # The second run is the one worth having. A receiver that counted nothing
      # would sail through the first. What is under test is not "nothing was
      # lost" but "nothing was lost without being counted", and that is only
      # testable by overloading the thing until it drops packets.
      #
      # No timing is asserted. A shared runner cannot support a latency claim
      # and the tool says so itself; the numbers come from a pinned host.
      - name: Wire-to-book pipeline loses nothing, and notices when it does
        env:
          CLEAN_RATE: 40000
        run: ./scripts/wire-to-book-check.sh build

      # ...and the gate must be able to fail. Told to expect a clean run from a
      # configuration that cannot deliver one, it has to report the loss rather
      # than accept it.
      - name: Wire-to-book gate self-test (it must reject a lossy run)
        run: |
          sed 's/CLEAN_RING:-16/CLEAN_RING:-10/; s/CLEAN_RATE:-60000/CLEAN_RATE:-3000000/' \
            scripts/wire-to-book-check.sh > /tmp/neg.sh
          chmod +x /tmp/neg.sh
          if PORT_OK=26601 PORT_LOSSY=26602 /tmp/neg.sh build > /tmp/neg.txt 2>&1; then
            cat /tmp/neg.txt
            echo "FAIL: the gate passed a run that dropped thousands of packets"
            exit 1
          fi
          grep -q "FAIL: exit 3, expected 0" /tmp/neg.txt
          echo "gate correctly rejected a lossy run"

      # Phase 10.6. The pipeline may reorder time; it may never reorder effect.
      #
      # Gate A: below the knee, the book two threads build over a socket must be
      # byte-identical to the one book_replay builds synchronously from the same
      # file -- across three ring sizes and two rates, because one configuration
      # matching proves the code ran and six matching proves the answer does not
      # depend on the schedule. Configurations that drop are above the knee on
      # this runner and are skipped BY NAME, with the gate failing if too few
      # stayed clean: a machine that dropped everything must not report a pass
      # having compared nothing.
      #
      # Gate B: above the knee, every message applied must be one the sender
      # sent, exactly once, in order -- checked against the SENDER's feed rather
      # than by replaying the recording, because a corrupted slot reproduces the
      # same wrong book on both sides and a book-to-book diff cannot see it.
      #
      # This is the gate that found the ring bug: publishing against a free
      # count writable() had not issued left its cached cursor past capacity,
      # the next call underflowed to 18 quintillion free slots, and messages
      # were applied out of order by exactly one lap. Nothing was lost, so every
      # count-based check passed.
      - name: Determinism gate (the pipeline's book equals the synchronous book)
        run: ./scripts/determinism-gate.sh build

      # ...and gate B has to be able to fail, on the exact bug it caught in
      # August 2026: under overload the ring got so full that the GAP MARKERS
      # could not be staged, and the consumer went on applying the messages
      # after them having never been told anything was missing. 10.10 made that
      # impossible -- a marker waits for a slot and nothing may pass it -- and
      # this proves the gate would still notice if it became possible again.
      #
      # --break-gap-markers restores the old discard behaviour and nothing else.
      # TORTURE_ONLY skips gate A, which this self-test does not exercise.
      # Phase 12.1 and 12.2. The exchange applies historical adds as state and
      # replays historical executions as crossing events through the matcher,
      # then PUBLISHES ITCH describing every mutation it made. Three books run
      # over one feed in one process -- the phase-9 path, the split replayer,
      # and a consumer rebuilding from the published feed -- and all three are
      # compared after every message, including a hash of the front of each
      # side's queue, because nothing else the gate compares can see intra-level
      # order and that is the one property 12.7 depends on.
      #
      # It runs on generated feeds here. The licensed-data leg is recorded in
      # validation/p1-emitted-2019-12-30.json.
      # Phase 12.6. A million operations of random OUCH order entry
      # interleaved with historical ITCH traffic against the SAME shared book,
      # under ASan/UBSan, with the cross-protocol differential checked at the
      # end of every sequence: the book a subscriber rebuilds from the
      # published feed must equal the exchange's own book, in both directions.
      #
      # Built with sanitizers deliberately, unlike fuzz_matcher -- that
      # opt-out is a throughput decision, and the cost here was measured at
      # ~110k ops/s, so a million operations is about two minutes.
      #
      # The binary FAILS if it never reached the two fill paths the phase
      # exists to differentiate (the strategy crossing historical liquidity,
      # and the aggressor hitting a resting strategy order). Both were
      # completely broken before 12.6, so a run that does not reach them is
      # not evidence of anything.
      - name: Gateway fuzz and cross-protocol differential (1M ops, ASan/UBSan)
        run: |
          g++ -std=c++20 -O2 -g -Wall -Wextra -Wpedantic -Werror \
              -fsanitize=address,undefined -fno-sanitize-recover=undefined \
              -Iinclude -I. tests/fuzz/fuzz_gateway.cpp -o /tmp/fuzz_gateway -lz
          /tmp/fuzz_gateway --iterations 30000

      # ...and it has to be able to fail. Suppressing the ITCH delete the
      # gateway publishes when an order is cancelled makes the subscriber's
      # book drift from the exchange's, which is exactly the class of bug the
      # differential exists to catch.
      - name: Differential self-test (it must catch a divergent feed)
        run: |
          cp include/itchbook/engine/gateway.hpp /tmp/gw.bak
          sed -i 's|if (was_in_book) emit_itch_delete(id);|(void)was_in_book;|' \
              include/itchbook/engine/gateway.hpp
          g++ -std=c++20 -O2 -Wall -Wextra -Iinclude -I. \
              tests/fuzz/fuzz_gateway.cpp -o /tmp/fuzz_neg -lz
          cp /tmp/gw.bak include/itchbook/engine/gateway.hpp
          if /tmp/fuzz_neg --iterations 500 > /tmp/neg.txt 2>&1; then
            cat /tmp/neg.txt
            echo "FAIL: the differential passed a feed that omits deletes"
            exit 1
          fi
          grep -q "different amount than the exchange" /tmp/neg.txt
          echo "the differential correctly refused a divergent feed"

      - name: Split replayer and P1 (the published feed rebuilds the same book)
        run: ./scripts/split-replay-gate.sh

      # ...and it has to be able to fail. One byte of one published message,
      # inside the body so it still routes, and the gate must notice. A gate
      # nobody has watched fail is a gate nobody should trust -- this repository
      # shipped a receiver that returned before reading its own counters and
      # reported a clean sheet every time.
      - name: P1 self-test (it must catch one corrupted published message)
        run: |
          python3 python/make_queue_feed.py --seed 7 --messages 200000 \
                  --locates 8 /tmp/p1self.gz >/dev/null
          if ./build/split_replay_gate /tmp/p1self.gz --break-emitter 5000 \
               > /tmp/p1neg.txt 2>&1; then
            tail -20 /tmp/p1neg.txt
            echo "FAIL: the gate passed a feed with a corrupted published message"
            exit 1
          fi
          grep -q "P1 divergence at message 5000" /tmp/p1neg.txt
          echo "the gate correctly refused a corrupted published feed"

      # Phase 12.7. Two OS processes and two real sockets: the exchange
      # publishes MoldUDP64 over UDP and speaks OUCH over SoupBinTCP, and a
      # separate strategy process rebuilds the book from those datagrams,
      # quotes over TCP, and reports the fills it can see ON ITS OWN FEED.
      #
      # Three arms, and the third is the one that carries the claim. Arm 1 is a
      # required green baseline. Arm 2 has the exchange withhold exactly the
      # ITCH executions naming a strategy reference: the strategy must fall
      # silent WHILE the exchange still reports the fills happened, because a
      # detector that cannot see its input must report nothing. Arm 3 deletes
      # every OUCH Executed inside the strategy: the fills must survive, since
      # they never came from there.
      #
      # Every arm also cross-checks the shares the exchange's aggressor took
      # against the shares the strategy counted off the tape. Those two numbers
      # are computed in different address spaces from different inputs and
      # there is no path by which a bug in one produces the other.
      - name: Closed loop (two processes, UDP feed, TCP order entry)
        run: ./scripts/closed-loop-check.sh

      # ...and it has to be able to fail. Five mutations were run against this
      # gate locally and all five were caught; this is the cheapest of them,
      # and the only one that fits in a CI step: the strategy reports the OUCH
      # count where it means the feed count. One wrong variable inverts the
      # entire claim, and arms 2 and 3 both refuse it.
      - name: Closed-loop self-test (it must catch OUCH reported as the feed)
        run: |
          cp tools/strategy.cpp /tmp/strategy.bak
          sed -i 's|belief.maker_fills, belief.maker_fills_in_book, belief.maker_fill_shares,|ouch_executed_received, belief.maker_fills_in_book, ouch_executed_shares,|' \
              tools/strategy.cpp
          grep -q "ouch_executed_received, belief.maker_fills_in_book" tools/strategy.cpp
          cmake --build build --target strategy -j 2
          cp /tmp/strategy.bak tools/strategy.cpp
          if TCP_PORT=27801 UDP_PORT=27802 ./scripts/closed-loop-check.sh \
               > /tmp/cl-neg.txt 2>&1; then
            tail -30 /tmp/cl-neg.txt
            echo "FAIL: the gate passed a strategy reporting OUCH as feed fills"
            cmake --build build --target strategy -j 2
            exit 1
          fi
          grep -q "FAIL" /tmp/cl-neg.txt
          cmake --build build --target strategy -j 2
          echo "the gate correctly refused fills that came from OUCH"

      - name: Determinism gate self-test (it must catch an unannounced loss)
        run: |
          if TORTURE_ONLY=1 TORTURE_EXTRA=--break-gap-markers PORT=26700 \
               ./scripts/determinism-gate.sh build > /tmp/det-neg.txt 2>&1; then
            cat /tmp/det-neg.txt
            echo "FAIL: the gate passed a run where the book was never told about the loss"
            exit 1
          fi
          grep -q "never announced" /tmp/det-neg.txt
          echo "the gate correctly refused a run that lost track of its own loss"

      # Phase 10.7. A three-rung ladder, two runs each, purely to keep the
      # sweep path alive: it asserts that the ladder anchors to the feed's own
      # clock, that every rate produces the columns the report needs, and that
      # both figures render. It asserts NOTHING about the timings, because a
      # two-core shared runner cannot support a latency claim and the sweep says
      # so itself. The real curve comes from a pinned host and is committed as
      # validation/rate-sweep.json.
      #
      # --allow-unqualified, because the sweep now EXITS 3 when no rate on the
      # ladder had a sender that held its 10 us schedule, and this runner never
      # will. The flag is passed here and nowhere else: a measurement run that
      # wanted the same exemption would have to ask for it in the open.
      - name: Rate sweep runs end to end and both figures render
        run: |
          python3 bench/rate-sweep.py --build build --out /tmp/sweep.json \
            --messages 60000 --session-seconds 20 --repeats 1 \
            --multipliers 5,20,50 --extend 0 --allow-unqualified
          python3 - <<'PY'
          import json, sys
          d = json.load(open('/tmp/sweep.json'))
          assert d['feed']['real_time_msg_per_s'] > 0, 'the ladder was not anchored'
          assert len(d['rows']) == 3, d['rows']
          for r in d['rows']:
              for k in ('offered_rate', 'wire_to_book_ns', 'ring_full_drops',
                        'kernel_drops', 'staging_overflow', 'peak_ring_occupancy',
                        'lossy', 'sender_late', 'sender'):
                  assert k in r, (k, sorted(r))
          # A sweep that reached the top of its ladder clean must SAY the
          # sustainable rate is a lower bound rather than report it as measured.
          if d['max_sustainable'] and not d['cliff_reached']:
              assert d['max_sustainable']['is_lower_bound'], d['max_sustainable']
          print('sweep shape ok;',
                d['sender_qualified_rates'], 'of', d['rates_tried'],
                'rates had the sender holding its schedule')
          PY
          python3 python/analysis/rate_latency.py /tmp/sweep.json \
            --svg /tmp/rate-latency.svg --hist-svg /tmp/wtb-hist.svg
          for f in /tmp/rate-latency.svg /tmp/wtb-hist.svg; do
            test -s "$f"; grep -q '<svg' "$f"; grep -q '</svg>' "$f"
          done

      # Standing rule 7 for phase 10: the results doc's tables are generated
      # from the committed sweep, and this fails if the prose has drifted.
      - name: The phase 10 results match their artifacts
        run: |
          python3 scripts/phase10-report.py --check
          python3 scripts/phase10-8-report.py --check

      # Phase 10.8. The reader thread must be INDISTINGUISHABLE from the
      # single-threaded path, not merely fast: same message stream, same book,
      # same failures on the same malformed input. test_reader_thread covers the
      # stream (and runs under ASan/UBSan here and under TSan in the other job);
      # this covers the book, across all three chunk sizes, because the chunk
      # size is what decides where messages get carried across a slot boundary.
      #
      # It asserts nothing about speed. The overlap measurement needs a machine
      # with cores to spare and lives in validation/reader-overlap.json.
      - name: Reader thread builds the identical book at every chunk size
        run: |
          python3 scripts/make-synthetic-feed.py data/raw/overlap.gz \
            --messages 200000 --symbols 64
          ./build/book_replay data/raw/overlap.gz --all-symbols \
            --per-symbol /tmp/seq.csv --json /tmp/seq.json --quiet
          for kb in 64 256 1024; do
            ./build/book_replay data/raw/overlap.gz --all-symbols --reader-thread \
              --reader-chunk-kb $kb --per-symbol /tmp/thr-$kb.csv \
              --json /tmp/thr-$kb.json --quiet
            cmp /tmp/seq.csv /tmp/thr-$kb.csv
            echo "chunk ${kb}KB: identical book"
          done
          python3 - <<'PY'
          import json
          seq = json.load(open('/tmp/seq.json'))
          for kb in (64, 256, 1024):
              t = json.load(open(f'/tmp/thr-{kb}.json'))
              assert t['reader_thread'] is True, kb
              assert t['messages_read'] == seq['messages_read'], (kb, t['messages_read'])
              assert t['reader_chunks'] > 0, kb
              # Both counters have to be reported; a bottleneck nobody can name
              # is a benchmark nobody can act on.
              assert 'producer_stalls' in t and 'consumer_stalls' in t, kb
          print('all three chunk sizes read the same message count as the '
                'single-threaded path')
          PY

      # ...and a message that cannot fit in a chunk must SAY so rather than
      # hang. The first version of the producer reserved a 65,535-byte maximum
      # message inside a 65,536-byte chunk, so it broke out before reading a
      # byte and spun forever; the second memcpy'd a carried message into a
      # smaller chunk and overflowed the heap, which only ASan saw.
      - name: An oversized message is refused, not hung on
        run: |
          python3 - <<'PY'
          import gzip, struct
          with gzip.open('data/raw/oversize.gz', 'wb') as f:
              for payload in (b'z' + b'x' * 39, b'z' + b'x' * 900):
                  f.write(struct.pack('>H', len(payload)))
                  f.write(payload)
          PY
          # A 902-byte frame against a 64 KB chunk is fine; the tests cover the
          # refusal path at chunk sizes the flag cannot reach, so here we only
          # require that a large frame still round-trips rather than hanging.
          timeout 60 ./build/book_replay data/raw/oversize.gz --all-symbols --reader-thread \
            --json /tmp/oversize.json --quiet
          python3 -c "
          import json
          d = json.load(open('/tmp/oversize.json'))
          assert d['messages_read'] == 2, d['messages_read']
          print('a 902-byte frame read through the reader thread without hanging')
          "

      - name: Regression gate (the same feed reconstructs to the same bytes)
        run: ./scripts/regression-gate.sh

      - name: All symbols equals every symbol one at a time
        run: |
          python3 python/make_queue_feed.py data/raw/multi.gz --messages 200000 \
            --locates 8 --seed 5
          ./build/book_replay data/raw/multi.gz --all-symbols \
            --per-symbol /tmp/per_symbol.csv | tee /tmp/all.txt
          python3 python/analysis/all_symbols_check.py data/raw/multi.gz \
            --binary build/book_replay --band-sweep 8 64 4096
          python3 - <<'PY'
          import re, sys
          text = open('/tmp/all.txt').read()
          def field(name):
              m = re.search(name + r'\s+([\d,]+)', text)
              return int(m.group(1).replace(',', '')) if m else None
          for name in ('unknown references', 'locate mismatches', 'undirectoried messages'):
              value = field(name)
              if value != 0:
                  sys.exit(f"{name} = {value}, must be 0 on a well-formed feed")
          if field('books built') != 8:
              sys.exit(f"expected 8 books, got {field('books built')}")
          print('all-symbols counters clean')
          PY
          ./build/itch_slice data/raw/multi.gz SY0003 data/sliced/SY0003.gz
          ./scripts/full-day-differential.sh data/sliced/SY0003.gz 1000 10

      # The other half of verification at scale. The oracle proves a symbol
      # right one symbol at a time and cannot chew a whole feed; these catch
      # what sampling misses -- a dropped message type, a double-counted
      # locate, volume lost between symbols. All correct per symbol, all wrong
      # in aggregate, all invisible to a per-symbol differential.
      - name: Global invariants (the census and the book agree in aggregate)
        run: |
          ./build/itch_census data/raw/multi.gz --peak-orders \
            --per-symbol /tmp/census.json
          ./build/book_replay data/raw/multi.gz --all-symbols \
            --json /tmp/run.json --per-symbol /tmp/per.csv --quiet
          python3 scripts/full-day-check.py --census /tmp/census.json \
            --run /tmp/run.json --per-symbol /tmp/per.csv

      # 9.12's sampled differential. The seed is PINNED here and rotates
      # locally, because CI's job is to fail on a regression rather than to
      # explore -- a job that draws new symbols every push fails for a new
      # reason every push. There is no market data here, so it samples the
      # generated multi-locate feed; the real-day run and its seed are recorded
      # in validation/sampled-differential-2019-12-30.json.
      #
      # --interval-ms 10 because this feed's session is seconds long, not hours.
      # At a coarser interval every snapshot CSV comes out empty, and two empty
      # files are identical -- which this script now reports as VOID rather than
      # as a pass, after doing exactly that on its first run.
      - name: Sampled differential (K symbols, seeded, oracle vs C++)
        run: |
          python3 scripts/sampled-differential.py data/raw/multi.gz \
            --symbols-from /tmp/per.csv --seed 20191230 --k 8 \
            --build build --interval-ms 10 --out /tmp/sampled \
            --slices /tmp/slices --json /tmp/sampled.json

      - name: Smoke test (reference book replay)
        run: |
          python3 python/reference/replay.py data/raw/sample.gz --symbol TEST \
            --snapshots data/sliced/TEST_book.csv --interval-ms 5
          head -3 data/sliced/TEST_book.csv

      # The phase 3 done-condition: the C++ book and the Python oracle must
      # produce byte-identical snapshots. Not "close" — identical.
      - name: Differential test (sample feed)
        run: |
          ./build/book_replay data/raw/sample.gz --symbol TEST \
            --snapshots data/sliced/TEST_book_cpp.csv --interval-ms 5
          python3 python/analysis/book_diff.py \
            data/sliced/TEST_book.csv data/sliced/TEST_book_cpp.csv

      - name: Differential test (adversarial feeds)
        run: python3 tests/differential.py --binary build/book_replay --seeds 5

      # ctest already runs a short soak. This is a longer one, still bounded so
      # a push does not wait minutes for it.
      - name: Property fuzz the matching engine
        run: ./build/fuzz_matcher --iterations 1000000 --seed 1

      - name: Property fuzz the queue model
        run: ./build/fuzz_queue --iterations 200000 --seed 1

      # Phase 3's done-condition at the scale it was stated: "bit-identical
      # output to the Python oracle across a full trading day. Not 'close.'
      # Identical." Every other differential in this file runs on tens of
      # thousands of messages, which catches logic errors and cannot catch
      # anything that only appears after hours of session — a counter that
      # drifts, an accumulator that overflows, a path reached twice a day.
      # 1.2M messages is one liquid name for one day.
      - name: Full-day differential (1.2M messages, C++ vs the Python oracle)
        run: |
          python3 python/make_queue_feed.py data/raw/fullday.gz \
            --messages 1200000 --gap-ns 20000 --seed 3
          ./scripts/full-day-differential.sh data/raw/fullday.gz 1000 10

      # The degenerate day: orders entered and pulled, nothing ever trades, and
      # the book is empty at the close. Every quantity the summary reports is
      # then UNDEFINED — no touch, no OHLC, no VWAP — and the two
      # implementations have to spell that the same way. They did not: the C++
      # side wrote its -1 sentinels and a raw NUL for the trading state, which
      # is not merely a different value but an unparseable JSON file. Only a
      # real day surfaced it, because a generated feed ends with orders still
      # resting and something having traded.
      - name: Degenerate-day differential (empty book, no trades)
        run: |
          python3 -c "
          import gzip, sys
          sys.path.insert(0, 'python')
          import make_sample as gen
          t = gen.T0
          out = bytearray()
          for p in (gen.system_event(t, b'O'), gen.stock_directory(t, gen.SYMBOL),
                    gen.system_event(t, b'Q'),
                    gen.add_order(t + 1000, 1, b'B', 100, 1000000),
                    gen.add_order(t + 2000, 2, b'S', 100, 1000200),
                    gen.order_delete(t + 3000, 1), gen.order_delete(t + 4000, 2),
                    gen.system_event(t + 5000, b'M'), gen.system_event(t + 6000, b'C')):
              out.extend(gen.frame(p))
          gzip.open('data/raw/degenerate.gz', 'wb').write(bytes(out))
          "
          ./scripts/full-day-differential.sh data/raw/degenerate.gz 1000 10

      # Phase 11.0. The closed-loop driver orchestrates the same components as
      # backtest.hpp in the same order, and that similarity is the liability:
      # two copies of one piece of logic either disagree on a trailing zero or
      # agree while both being wrong. The invariant is that a strategy WITHOUT
      # feedback is answering the same question in both drivers, so both must
      # answer it identically, field for field, across all four fill models.
      # It runs in ctest; this step is here to name it in the log, because a
      # gate nobody can see in CI output is a gate people forget exists.
      - name: Closed-loop driver agrees with the open-loop one without feedback
        run: ./build/test_closed_loop

      # Phase 11.1. The A-S algebra checked against the paper, the volatility
      # estimator against known inputs, and the unit trap locked in: the paper's
      # own gamma and k produce a ~170-tick spread on a penny-wide book, which
      # is how the first run of this managed to quote all day and never fill.
      - name: Avellaneda-Stoikov model and its units
        run: ./build/test_as_maker

      # Phase 11.2. The estimator is checked by generating data from a known A
      # and k and requiring them back; the recorder is checked one exclusion at
      # a time, because each thing it refuses to count would bias k in a stated
      # direction if it were counted.
      - name: Fill-intensity estimator recovers a known curve
        run: ./build/test_intensity

      # ...and the calibration runs end to end and renders. No number is
      # asserted: this feed is generated, and lambda(delta) measured on a
      # synthetic book is a property of the generator. The real calibration is
      # one trading day and is committed as an artifact.
      - name: Intensity calibration runs and the figure renders
        run: |
          python3 python/make_queue_feed.py data/raw/calib.gz --messages 200000 \
            --seed 7 --gap-ns 20000
          ./build/calibrate_intensity data/raw/calib.gz --json /tmp/calib.json
          python3 - <<'PY'
          import json
          d = json.load(open('/tmp/calib.json'))
          assert d['calibrated_per_lane'] is True
          # Four separate runs, because once the strategy sees its fills the
          # lanes are four different worlds with four different denominators.
          assert set(d['lanes']) == {'naive', 'optimistic', 'mbo', 'pessimistic'}, d['lanes']
          for name, lane in d['lanes'].items():
              for key in ('fit_ok', 'A', 'k', 'r_squared', 'buckets_fitted',
                          'buckets_no_fills', 'maker_fills',
                          'exposure_order_seconds', 'buckets', 'residuals'):
                  assert key in lane, (name, key)
          fitted = [n for n, l in d['lanes'].items() if l['fit_ok']]
          assert fitted, 'no lane produced a usable fit'
          print('fitted lanes:', ', '.join(sorted(fitted)))
          PY
          python3 python/analysis/intensity_fit.py /tmp/calib.json \
            --svg /tmp/intensity.svg --lane mbo
          test -s /tmp/intensity.svg
          grep -q '</svg>' /tmp/intensity.svg

      # Phase 11.3. The experiment harness end to end: three arms x four lanes x
      # a gamma sweep, over two generated "days", with the calibration day
      # excluded from evaluation. No number is asserted -- these feeds are
      # generated, so any P&L is a property of the generator. What IS asserted
      # is the experimental design: three arms present, calibration and
      # evaluation never sharing a day, per-symbol rows preserved, and both
      # sweep figures rendering.
      - name: A-S experiment harness, three arms and the gamma sweep
        run: |
          python3 python/make_queue_feed.py data/raw/d1.gz --messages 120000 \
            --seed 11 --gap-ns 20000
          python3 python/make_queue_feed.py data/raw/d2.gz --messages 120000 \
            --seed 12 --gap-ns 20000
          ./build/calibrate_intensity data/raw/d1.gz --symbol TEST --day 2019-12-30 \
            --json /tmp/cal-TEST.json
          python3 bench/as-experiment.py --build build --out /tmp/asexp.json \
            --feed TEST:2019-12-30:data/raw/d1.gz \
            --feed TEST:2020-01-30:data/raw/d2.gz \
            --calibration TEST:/tmp/cal-TEST.json \
            --calibration-day 2019-12-30 --gammas 0.001,0.005,0.02,0.1
          python3 - <<'PY'
          import json
          d = json.load(open('/tmp/asexp.json'))
          arms = {r['arm'] for r in d['runs']}
          assert arms == {'symmetric-touch', 'as-gamma0', 'as'}, arms
          # The control arm is the whole reason this is a three-way comparison.
          assert 'as-gamma0' in arms, 'the gamma=0 control is missing'
          assert d['calibration_day'] not in d['evaluation_days'], \
              'calibration and evaluation share a day'
          # Rows keep their symbol and day: no pooling.
          for r in d['runs']:
              assert r['symbol'] and r['day'] and 'is_calibration_day' in r
          models = {r['model'] for r in d['runs']}
          assert models == {'naive', 'optimistic', 'mbo', 'pessimistic'}, models
          # k reached the experiment from the calibration artifact, per lane.
          # It used to be one scalar for every lane and every symbol, which
          # silently discarded the section 6.1 conditioning the paper claims.
          assert d['k_source'] == 'measured-per-lane', d['k_source']
          ks = d['k_by_symbol']['TEST']
          assert set(ks) == {'naive', 'optimistic', 'mbo', 'pessimistic'}, ks
          assert len(set(round(v, 6) for v in ks.values())) > 1, \
              f'all four lanes got the same k: {ks}'
          for r in d['runs']:
              if r['arm'] == 'symmetric-touch':
                  # A touch maker never evaluates the spread formula, so no
                  # intensity curve enters the row. 0 is a statement, not a gap.
                  assert r['k'] == 0, r
              else:
                  assert abs(r['k'] - ks[r['model']]) < 1e-6, r
          assert d['calibration_artifacts']['TEST']['sha256']
          print(len(d['runs']), 'runs across', len(d['symbols']), 'symbol(s);',
                'k per lane:', {m: round(v, 1) for m, v in ks.items()})
          PY
          python3 python/analysis/gamma_sweep.py /tmp/asexp.json \
            --svg-inventory /tmp/g_inv.svg --svg-pnl /tmp/g_pnl.svg
          for f in /tmp/g_inv.svg /tmp/g_pnl.svg; do
            test -s "$f"; grep -q '</svg>' "$f"
          done

      # k is per symbol AND per lane, and the driver refuses every way of
      # quietly collapsing that. Each of these produced a silently-wrong
      # experiment before the refusals existed: one k across four lanes throws
      # away the section 6.1 conditioning the paper claims to perform, and one
      # k across symbols is indefensible once you measure that spreads on a
      # real session run from 1.0 ticks to 60.7.
      - name: The driver refuses every way of collapsing k
        run: |
          ./build/calibrate_intensity data/raw/d1.gz --symbol TEST --day 2019-12-30 \
            --json /tmp/cal-TEST.json
          fail() { echo "FAIL: $1"; exit 1; }

          # 1. No calibration at all, and no explicit opt-out.
          if python3 bench/as-experiment.py --build build --out /tmp/k1.json \
               --feed TEST:2019-12-30:data/raw/d1.gz \
               --feed TEST:2020-01-30:data/raw/d2.gz \
               --calibration-day 2019-12-30 --gammas 0.005 > /tmp/k1.txt 2>&1; then
            cat /tmp/k1.txt; fail "ran with no calibration"
          fi
          grep -q "no calibration for TEST" /tmp/k1.txt

          # 2. A calibration fitted on a day that is then evaluated.
          python3 -c "
          import json
          d = json.load(open('/tmp/cal-TEST.json')); d['day'] = '2020-01-30'
          json.dump(d, open('/tmp/cal-wrongday.json', 'w'))"
          if python3 bench/as-experiment.py --build build --out /tmp/k2.json \
               --feed TEST:2019-12-30:data/raw/d1.gz \
               --feed TEST:2020-01-30:data/raw/d2.gz \
               --calibration TEST:/tmp/cal-wrongday.json \
               --calibration-day 2019-12-30 --gammas 0.005 > /tmp/k2.txt 2>&1; then
            cat /tmp/k2.txt; fail "accepted a calibration from an evaluation day"
          fi
          grep -q "calibrated on 2020-01-30" /tmp/k2.txt

          # 3. A lane whose intensity could not be fitted. Borrowing another
          #    lane's k here is the collapse, so it must not be silent.
          python3 -c "
          import json
          d = json.load(open('/tmp/cal-TEST.json')); d['lanes']['mbo']['fit_ok'] = False
          json.dump(d, open('/tmp/cal-nofit.json', 'w'))"
          if python3 bench/as-experiment.py --build build --out /tmp/k3.json \
               --feed TEST:2019-12-30:data/raw/d1.gz \
               --feed TEST:2020-01-30:data/raw/d2.gz \
               --calibration TEST:/tmp/cal-nofit.json \
               --calibration-day 2019-12-30 --gammas 0.005 > /tmp/k3.txt 2>&1; then
            cat /tmp/k3.txt; fail "accepted a lane with no usable fit"
          fi
          grep -q "no usable fit for mbo" /tmp/k3.txt

          # 4. The escape hatch works, and stamps what it did.
          python3 bench/as-experiment.py --build build --out /tmp/k4.json \
            --feed TEST:2019-12-30:data/raw/d1.gz \
            --feed TEST:2020-01-30:data/raw/d2.gz \
            --calibration-day 2019-12-30 --gammas 0.005 --k 138 --allow-assumed-k \
            > /dev/null
          python3 -c "
          import json, sys
          d = json.load(open('/tmp/k4.json'))
          assert d['k_source'] == 'assumed-scalar', d['k_source']
          assert all(r['k'] in (0.0, 138.0) for r in d['runs']), 'assumed k not applied'
          print('escape hatch stamps itself:', d['k_source'])"

          # 5. The tool itself rejects a partial lane set rather than filling in.
          if ./build/as_experiment data/raw/d1.gz --symbol TEST --day 2019-12-30 \
               --k-per-lane naive=90,mbo=200 --gammas 0.005 > /tmp/k5.txt 2>&1; then
            cat /tmp/k5.txt; fail "accepted two lanes out of four"
          fi
          grep -q "all four lanes" /tmp/k5.txt
          echo "every collapse refused"

      # ...and the split has to be enforced, not documented. Evaluating on the
      # calibration day is the most comfortable mistake available here.
      - name: Calibration and evaluation must not share a day
        run: |
          if python3 bench/as-experiment.py --build build --out /tmp/bad.json \
               --feed TEST:2019-12-30:data/raw/d1.gz \
               --calibration-day 2019-12-30 --k 138 --gammas 0.005 \
               > /tmp/bad.txt 2>&1; then
            cat /tmp/bad.txt
            echo "FAIL: it evaluated on the day it calibrated on"
            exit 1
          fi
          grep -q "must not share a day" /tmp/bad.txt
          echo "refused, as it must"

      # Phase 11.4. The paper's results sections are GENERATED -- standing rule 7
      # applied to the paper itself -- and this checks both halves of that claim.
      #
      # First half: the committed paper matches its committed artifacts. There
      # are no evaluation artifacts yet, so what that currently asserts is the
      # REFUSAL: the generator must have written "no results" rather than left a
      # hole, and there must be no graded table on a page nothing backs.
      # The paper's results section must AGREE WITH WHETHER AN ARTIFACT EXISTS,
      # in both directions. The first version of this asserted only the refusal
      # -- that the paper says "No results" and carries no graded table -- which
      # was right while nothing was committed and became wrong the moment the
      # evaluation artifact landed. It then failed on a paper that was correct,
      # which is the worst kind of gate: one that has to be silenced to make
      # progress. Symmetric now, so neither state can pass by accident.
      - name: The paper's results agree with whether an artifact exists
        run: |
          ./scripts/paper-build.sh --check
          if [[ -f validation/as-experiment.json ]]; then
            grep -q "The predictions, graded" docs/paper/as-on-itch.md \
              || { echo "FAIL: an evaluation artifact is committed but the paper has no graded table"; exit 1; }
            if grep -q "^> \*\*No results\.\*\*" docs/paper/as-on-itch.md; then
              echo "FAIL: the paper claims no results while an artifact is committed"
              exit 1
            fi
            grep -q "^### 8.1 The answer" docs/paper/as-on-itch.md \
              || { echo "FAIL: an artifact is committed but the paper has no conclusion"; exit 1; }
            if grep -q "Not concluded" docs/paper/as-on-itch.md; then
              echo "FAIL: the paper declines to conclude while an artifact is committed"
              exit 1
            fi
            if grep -q "results pending data" docs/paper/as-on-itch.md; then
              echo "FAIL: the status banner says results are pending while they are not"
              exit 1
            fi
            echo "artifact present, paper reports results and concludes"
          else
            grep -q "No results" docs/paper/as-on-itch.md \
              || { echo "FAIL: no artifact, and the paper does not say so"; exit 1; }
            if grep -q "The predictions, graded" docs/paper/as-on-itch.md; then
              echo "FAIL: a graded prediction table with no artifact behind it"
              exit 1
            fi
            grep -q "Not concluded" docs/paper/as-on-itch.md \
              || { echo "FAIL: no artifact, and the conclusion does not say so"; exit 1; }
            if grep -q "^### 8.1 The answer" docs/paper/as-on-itch.md; then
              echo "FAIL: a conclusion with no artifact behind it"
              exit 1
            fi
            echo "no artifact, paper refuses to invent one"
          fi

      # Second half, and the one that will matter later: the generator has to
      # WORK when the artifacts exist. Today --check only ever walks the refusal
      # branch. The branch that emits the tables and grades seven pre-registered
      # predictions would sit unrun until the day real data arrives -- which is
      # the worst possible day to discover it throws on a key it never had.
      #
      # The artifacts here are the ones the two steps above generated from
      # synthetic feeds. NO NUMBER IS ASSERTED: they are generated feeds, so any
      # P&L is a property of the generator. What is asserted is that the paper
      # gets a graded table, that the grader reached all seven predictions, that
      # a run below the pre-registered scope SAYS so, and that the whole chain
      # is idempotent.
      - name: ...and generates them when the artifacts do exist
        run: |
          work=$(mktemp -d)
          git ls-files -z | while IFS= read -r -d '' f; do
            mkdir -p "$work/$(dirname "$f")"; cp "$f" "$work/$f"
          done
          mkdir -p "$work/validation"
          cp /tmp/asexp.json "$work/validation/as-experiment.json"
          cp /tmp/calib.json "$work/validation/intensity.json"
          cd "$work"
          # Through the one entry point, in the one correct order. Running the
          # report before the figures writes a paper that omits figures about to
          # appear, which --check then calls stale.
          ./scripts/paper-build.sh
          python3 - <<'PY'
          import json, re, sys
          md = open('docs/paper/as-on-itch.md').read()
          block = md.split('<!-- generated:results:begin -->')[1] \
                    .split('<!-- generated:results:end -->')[0]
          assert '7.5 The predictions, graded' in block, 'no graded table'
          got = re.findall(r'^\| (P[1-7]) \|', block, re.M)
          assert got == [f'P{i}' for i in range(1, 8)], got
          # One symbol and one evaluation day is below the plan's bar, and the
          # paper has to say so rather than reading like a finished result.
          assert 'Below the pre-registered scope' in block, \
              'a one-symbol run did not announce that it is one symbol'
          # Every verdict is computed; none may be the empty string.
          verdicts = re.findall(r'\*\*(kept|falsified|mixed|not evaluated|'
                                r'TRIGGERED|not triggered)\*\* \|', block)
          assert len(verdicts) == 7, verdicts
          # The conclusion, the abstract's finding sentence and the status banner
          # are generated from the same artifact as the tables. A conclusion is
          # the most-quoted part of a paper and the likeliest to be left behind
          # by its own numbers, so it gets the same treatment and the same check.
          concl = md.split('<!-- generated:conclusion:begin -->')[1] \
                    .split('<!-- generated:conclusion:end -->')[0]
          for h in ('8.1 The answer', '8.2 Which half', '8.3 The mechanism',
                    '8.5 What remains'):
              assert h in concl, (h, concl[:400])
          assert 'Not concluded' not in concl, 'refused to conclude with an artifact present'
          find = md.split('<!-- generated:findings:begin -->')[1] \
                   .split('<!-- generated:findings:end -->')[0]
          assert 'symbol-day-lane cells' in find, find
          stat = md.split('<!-- generated:status:begin -->')[1] \
                   .split('<!-- generated:status:end -->')[0]
          assert 'Status: run.' in stat, stat
          # A one-symbol synthetic run is below the pre-registered scope, and the
          # banner has to say so rather than reading like a finished paper.
          assert 'BELOW the pre-registered scope' in stat, stat
          cal = md.split('<!-- generated:calibration:begin -->')[1] \
                  .split('<!-- generated:calibration:end -->')[0]
          assert 'calibrated_per_lane' in cal, 'the per-lane decision vanished'
          man = json.load(open('docs/figures/paper/manifest.json'))
          assert len(man['figures']) == 3, man
          for f in man['figures']:
              assert f['artifact_sha256'] and f['subject'], f
          h = open('docs/paper/as-on-itch.html').read()
          assert h.count('<svg') == 3, 'figures did not inline into the page'
          assert '<!--img' not in h, 'an image reference survived rendering'
          print('paper generated:', len(got), 'predictions graded,',
                len(man['figures']), 'figures with provenance')
          PY
          # Idempotent: running the build again must change nothing.
          ./scripts/paper-build.sh --check

      # P4 is the one prediction that cannot be graded from a single artifact:
      # it compares equity at two modelled latencies, so it reads every
      # validation/as-experiment*.json and keys them by latency_ns. Only one is
      # committed, so the branch that GRADES it has never run -- which is the
      # same "first executed on the day the real data arrives" problem the step
      # above exists to solve, one prediction deeper.
      #
      # No verdict is asserted: these are generated feeds. What is asserted is
      # that P4 stops reading "not evaluated", that the bar names both
      # latencies, and that the conclusion's "what remains" stops asking for a
      # second latency it now has.
      - name: ...and P4 grades once a second latency exists
        run: |
          python3 bench/as-experiment.py --build build --out /tmp/asexp-lat.json \
            --feed TEST:2019-12-30:data/raw/d1.gz \
            --feed TEST:2020-01-30:data/raw/d2.gz \
            --calibration TEST:/tmp/cal-TEST.json \
            --calibration-day 2019-12-30 --gammas 0.001,0.005,0.02,0.1 \
            --latency-ns 500000 > /dev/null
          work=$(mktemp -d)
          git ls-files -z | while IFS= read -r -d '' f; do
            mkdir -p "$work/$(dirname "$f")"; cp "$f" "$work/$f"
          done
          mkdir -p "$work/validation"
          cp /tmp/asexp.json "$work/validation/as-experiment.json"
          cp /tmp/asexp-lat.json "$work/validation/as-experiment-500us.json"
          cp /tmp/calib.json "$work/validation/intensity.json"
          cd "$work"
          ./scripts/paper-build.sh
          python3 - <<'PY'
          import re
          md = open('docs/paper/as-on-itch.md').read()
          row = re.search(r'^\| P4 \|.*$', md, re.M)
          assert row, 'no P4 row at all'
          row = row.group(0)
          assert 'not evaluated' not in row, row
          assert '500,000 ns' in row and '0 to' in row, row
          verdict = re.search(r'\*\*(kept|falsified|mixed)\*\* \|$', row)
          assert verdict, row
          assert 'Latency sensitivity — done.' in md, 'the conclusion still asks for it'
          # The edge-side companion must appear WITH the verdict, never instead
          # of it: P4's bar is equity because that is how it was pre-registered,
          # and the companion exists so the noisier instrument is not the only
          # one reported. Both, or the report is doing the thing it warns about.
          assert "P4's bar is equity" in md, 'no edge-side companion beside P4'
          for arm in ('`symmetric-touch`', '`as-gamma0`', '`as`'):
              assert arm in md, arm
          assert re.search(r'edge degrades faster than the baseline.s in \*\*\d+ of \d+\*\*',
                           md), 'the companion did not report a count'
          print('P4 graded across two latencies:', verdict.group(1),
                '-- with the edge-side companion beside it')
          PY
          ./scripts/paper-build.sh --check

      # A figure with no artifact behind it is a picture nothing can reproduce
      # or refute, so paper-figures.sh treats it as a failure rather than a
      # skip. That guard is the only thing standing between this paper and a
      # stale chart outliving the data that made it.
      - name: A figure whose artifact is gone is a failure, not a skip
        run: |
          work=$(mktemp -d)
          git ls-files -z | while IFS= read -r -d '' f; do
            mkdir -p "$work/$(dirname "$f")"; cp "$f" "$work/$f"
          done
          mkdir -p "$work/docs/figures/paper"
          echo '<svg></svg>' > "$work/docs/figures/paper/gamma-pnl.svg"
          # AND REMOVE THE ARTIFACT, which is what makes the figure an ORPHAN
          # rather than merely stale. Once validation/as-experiment.json was
          # committed, the copy above carried it along and --check reported a
          # figure that did not match its artifact -- a true statement, and not
          # the one under test. Same shape as the "No results" assertion that
          # inverted the day the results landed: a self-test written for the
          # empty world has to be updated when the world stops being empty.
          rm -f "$work"/validation/as-experiment.json "$work"/validation/intensity*.json
          cd "$work"
          if ./scripts/paper-figures.sh --check > /tmp/orphan.txt 2>&1; then
            cat /tmp/orphan.txt
            echo "FAIL: an orphaned figure passed the check"
            exit 1
          fi
          grep -q "a figure nothing can reproduce" /tmp/orphan.txt
          echo "orphan caught, as it must be"

      # Two independent implementations of the queue rules over identical bytes.
      # A queue model is a counterfactual with no ground truth, so agreement
      # between them is most of the assurance available before real data.
      - name: Differential test (queue model vs Python oracle)
        run: python3 tests/queue_differential.py --binary build/queue_sim --seeds 2

      # The framing layer must be transparent when nothing is wrong. Unit tests
      # build their own packets, so only pushing a real feed through the whole
      # path and comparing bytes catches a receiver that quietly drops one
      # message in ten thousand.
      - name: MoldUDP64 round trip (wrap, replay, compare bytes)
        run: |
          python3 python/make_queue_feed.py data/raw/rt.gz --seed 5 --messages 30000
          ./build/mold_wrap data/raw/rt.gz data/raw/rt.mold.gz --heartbeat-ns 1000000000
          ./build/mold_replay data/raw/rt.mold.gz --unwrap data/raw/rt.back.gz
          zcat data/raw/rt.gz > /tmp/rt-a.bin
          zcat data/raw/rt.back.gz > /tmp/rt-b.bin
          cmp /tmp/rt-a.bin /tmp/rt-b.bin
          echo "round trip identical: $(wc -c < /tmp/rt-a.bin) bytes"

      # Recovery, end to end. Knock a 20-packet hole in a wrapped feed — a
      # mid-day outage, roughly 800 consecutive messages — and require that
      # rebuild-forward converges back to a book byte-identical to the
      # undamaged one, and that the convergence detector notices.
      - name: Gap recovery (outage, rebuild, converge, compare books)
        run: |
          python3 python/make_queue_feed.py data/raw/rec.gz --seed 11 --messages 200000 \
            --gap-ns 300000
          ./build/mold_wrap data/raw/rec.gz data/raw/rec.mold.gz
          ./build/mold_damage data/raw/rec.mold.gz data/raw/rec.dmg.gz \
            --disconnect-at 5000 --disconnect-for 20
          ./build/mold_replay data/raw/rec.mold.gz --book-out /tmp/truth.csv --quiet
          # Exit 3 means messages were lost, which is the point of this run.
          ./build/mold_replay data/raw/rec.dmg.gz --book-out /tmp/recovered.csv \
            --quiet --recover-after 20000 > /tmp/recovered.txt || test $? -eq 3
          cat /tmp/recovered.txt
          grep -q "state                 trusted" /tmp/recovered.txt \
            || (echo "FAIL: never converged back to trusted" && exit 1)
          grep -q "recoveries            1" /tmp/recovered.txt \
            || (echo "FAIL: expected exactly one recovery" && exit 1)
          cmp /tmp/truth.csv /tmp/recovered.csv
          echo "recovered book identical to truth after an 800-message outage"

      # A restart is only worth having if it is transparent. Snapshot halfway
      # through a real feed, throw the book away, restore from the file, and
      # require the end state to match a run that never died — every order at
      # every price in queue order, plus the tape. The same step enforces the
      # conditional cross invariant: crossed while halted is legal and counted,
      # crossed while trading fails the build.
      - name: Restart recovery (snapshot, restore, compare to a run that never died)
        run: |
          ./build/restart_check data/raw/rec.gz --snapshot-file /tmp/mid.snap

      # Two quadratics lived in the simulator from the moment it was written
      # until a real 1.2M-message day exposed them — a six-point latency sweep
      # took the better part of an hour. Every existing test passed throughout,
      # because they are all about correctness and a quadratic is not wrong,
      # only ruinous. This asserts on the RATIO between two feed sizes rather
      # than a wall clock, so the runner's speed cancels out: double the input
      # and linear costs ~2x, quadratic ~4x. The pre-fix code measures 3.8x.
      - name: Scaling guard (the backtester must stay linear in feed length)
        run: python3 python/analysis/scaling_check.py --build build

      # The latency histogram is a build-plan deliverable and had no coverage
      # at all, so the whole path — bench -> bucket CSV -> SVG — could rot
      # without anything noticing until someone tried to regenerate the figure.
      # This asserts nothing about the timings, which mean nothing on a shared
      # runner; it asserts the pipeline still runs and produces a chart, in
      # both the single-run and the before/after form the plan asks for.
      - name: Latency histogram renders end to end
        run: |
          python3 python/make_bench_feed.py data/raw/hist.gz --messages 50000
          ./build/book_bench data/raw/hist.gz --histogram /tmp/after.csv
          ./build/book_bench data/raw/hist.gz --histogram /tmp/before.csv
          python3 python/analysis/latency_histogram.py /tmp/after.csv \
            --svg /tmp/latency.svg
          python3 python/analysis/latency_histogram.py /tmp/after.csv \
            --compare /tmp/before.csv --svg /tmp/latency-compare.svg
          for f in /tmp/latency.svg /tmp/latency-compare.svg; do
            test -s "$f"
            grep -q '</svg>' "$f"
            grep -q '<svg' "$f"
          done

      # The plan's testing table lists a Performance row whose stated purpose is
      # "Regressions", and for four phases it was measurement with nothing
      # comparing against it. This gates on the RATIO between two binaries built
      # here from the same source, differing only in the pool's chunk policy, so
      # the runner's speed cancels out. Thresholds are loose on purpose: 1.2x and
      # 10x against measured 1.6x and ~90x, because a gate set at the measured
      # value fails on noise and then gets disabled.
      - name: Performance regression gate (the pool change must still pay)
        run: python3 bench/regression_check.py --messages 300000

      # The phase 7 done-condition. Twelve scenarios of packet damage, each
      # graded CORRECT / SAFE / CAUTIOUS / WRONG. Any WRONG — the book diverged
      # while the system called itself trusted — fails the build. The feed here
      # is generated, not real: CI cannot carry licensed market data. The real
      # MSFT run is recorded in docs/phase7-results.md section 9.
      - name: Adversarial replay (drops, dups, reorders, truncation, an outage)
        run: python3 python/analysis/adversarial.py data/raw/rec.gz --build build

      # Phase 10's entry in the phase-7 matrix. Every scenario above damages a
      # FILE; this one damages nothing. The feed is perfect and every packet
      # arrives -- the loss comes from the receiver dropping whole packets
      # because the ring is full, because the book thread has been told to take
      # longer per message than the wire allows.
      #
      # That is phase 10.5's promise under test: "backpressure degrades to
      # graded feed gaps rather than silent loss". Same sequencer, same gap
      # policy, same rebuild-forward, same four verdicts, and 0 WRONG is the
      # same bar. The rate is conservative and the ring large because the
      # control scenario must drop NOTHING on a shared runner -- a control that
      # drops is a control that proves nothing.
      - name: Adversarial replay, live pipeline (backpressure as a graded gap)
        run: |
          python3 python/analysis/adversarial.py data/raw/rec.gz --build build \
            --pipeline --pipeline-rate 20000 --pipeline-ring-log2 12 \
            --pipeline-kill-occupancy 1000

      # ...and the live table has to be able to say WRONG too. Setting the
      # convergence bar to a single clean reference makes the pipeline call
      # itself trusted while its book is still missing thousands of orders,
      # which is precisely the silently-wrong state.
      - name: Live pipeline harness self-test (it must detect silent wrongness)
        run: |
          if python3 python/analysis/adversarial.py data/raw/rec.gz --build build \
               --pipeline --pipeline-rate 20000 --pipeline-ring-log2 12 \
               --recover-after 1 > /tmp/pipe-selftest.txt 2>&1; then
            cat /tmp/pipe-selftest.txt
            echo "FAIL: the live harness passed a run it should have failed"
            exit 1
          fi
          grep -q "consumer-slow" /tmp/pipe-selftest.txt
          grep -qE "WRONG" /tmp/pipe-selftest.txt
          echo "live harness correctly reported silent wrongness at a bar of 1"

      # ...and prove the harness can actually fail, because a grader that only
      # ever returns CORRECT is worth nothing. Setting the convergence bar to a
      # single clean reference makes the system call itself trusted while its
      # book is still missing orders, which is precisely the silently-wrong
      # state, and the harness must catch it.
      - name: Adversarial harness self-test (it must detect silent wrongness)
        run: |
          if python3 python/analysis/adversarial.py data/raw/rec.gz --build build \
               --recover-after 1 > /tmp/selftest.txt 2>&1; then
            cat /tmp/selftest.txt
            echo "FAIL: the harness passed a run it should have failed"
            exit 1
          fi
          grep -q "WRONG" /tmp/selftest.txt
          echo "harness correctly reported silent wrongness at a bar of 1"

      # A feed with a closed form. Every number below is fixed by construction
      # before the simulator runs, so a disagreement is a defect rather than an
      # opinion — and all four models must agree, because the feed contains no
      # queue ambiguity for them to differ about.
      - name: Closed-form check (toxic feed)
        run: |
          python3 python/make_toxic_feed.py data/raw/toxic.gz --episodes 40
          ./build/queue_backtest data/raw/toxic.gz --strategy touch-maker \
            --json data/raw/toxic.json
          python3 python/make_toxic_feed.py --check data/raw/toxic.json --episodes 40

      # The one EXTERNAL check in phase 6: shadow real orders and grade the
      # models against what those orders actually filled. Partial fills only,
      # because an order that ended fully executed grades every model perfect
      # and measures nothing.
      - name: Leave-one-out oracle (fill models vs real orders)
        run: |
          python3 python/make_queue_feed.py data/raw/queue.gz --seed 3 --messages 40000
          python3 python/analysis/leave_one_out.py data/raw/queue.gz \
            --binary build/queue_sim --samples 40 --partial-only
